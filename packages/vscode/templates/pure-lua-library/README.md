# Pure Lua library

Runs a sandboxed Lua 5.2 entry script with a workspace `require()` library.
Use Test Explorer **Run** or `ic10 test`; local Debug is intentionally not
available for pure Lua modules.
